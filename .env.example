# Base URL of the AWS box running the verification engine's admin API.
# Defaults to the production EC2 instance if unset — override for local
# testing against a different backend (e.g. http://127.0.0.1:5050/api/v1/admin).
AWS_ADMIN_API_BASE=https://18.185.59.156/api/v1/admin
